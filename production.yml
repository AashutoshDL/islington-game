services:
  islington-game:
    build:
      context: .
      dockerfile: docker/production/Dockerfile
    container_name: islington-game
    ports:
      - "3003:3003"
    networks:
      - islington-game-network

networks:
  islington-game-network:
    driver: bridge
